const api=require('../../../services/api');Page({data:{id:0,receiver:'',phone:'',region:['北京市','北京市','朝阳区'],detail:'',isDefault:true},async onLoad(o){const id=Number(o.id||0);this.setData({id});if(id){try{const list=await api.addresses();const a=list.find(x=>x.id===id);if(a)this.setData({receiver:a.receiver,phone:a.phone,region:[a.province,a.city,a.district],detail:a.detail,isDefault:a.isDefault})}catch{}}},field(e){this.setData({[e.currentTarget.dataset.k]:e.detail.value})},region(e){this.setData({region:e.detail.value})},toggle(e){this.setData({isDefault:e.detail.value})},async save(){const d=this.data;if(!d.receiver||!d.phone||!d.detail)return wx.showToast({title:'请填写完整信息',icon:'none'});const payload={receiver:d.receiver,phone:d.phone,province:d.region[0],city:d.region[1],district:d.region[2],detail:d.detail,isDefault:d.isDefault};try{d.id?await api.updateAddress(d.id,payload):await api.createAddress(payload);wx.showToast({title:'已保存'});setTimeout(()=>wx.navigateBack(),300)}catch(e){wx.showToast({title:e.message,icon:'none'})}}});
