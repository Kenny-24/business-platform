.nav,
.nav-spacer,
.nav__inner,
.nav__center,
.nav__title,
.nav__brand-image,
.nav__subtitle,
.nav__location,
.nav__location-icon,
.nav__location-text,
.nav__back,
.nav__back-icon {
  box-sizing: border-box;
}

.nav-spacer {
  width: 100%;
}

.nav {
  position: fixed;
  z-index: 1000;
  top: 0;
  left: 0;
  width: 100%;
  overflow: hidden;
  background: rgba(251, 250, 247, 0.98);
  transition:
    background-color 180ms ease,
    box-shadow 180ms ease;
}

.nav--transparent,
.nav--overlay {
  background: transparent;
}

.nav--solid {
  background: rgba(251, 250, 247, 0.98);
  box-shadow: 0 1rpx 0 rgba(72, 66, 54, 0.08);
}

.nav__inner {
  position: relative;
  width: 100%;
}

.nav__center {
  position: absolute;
  z-index: 1;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  display: flex;
  min-width: 0;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  pointer-events: none;
}

.nav__brand-image {
  display: block;
  width: 126rpx;
  height: 46rpx;
  transform: translateY(-9rpx);
}

.nav__title {
  max-width: 54%;
  overflow: hidden;
  color: #20201d;
  font-size: 31rpx;
  font-weight: 600;
  line-height: 1;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.nav__subtitle {
  margin-top: 8rpx;
  color: #7d786f;
  font-size: 20rpx;
  line-height: 1;
  white-space: nowrap;
}

/*
 * 使用 view 而不是原生 button，彻底移除真机按钮默认触摸背景。
 * 可见箭头距离屏幕左侧约 18～20px，点击区域保持 40px 左右。
 */
.nav__back {
  position: absolute;
  z-index: 3;
  top: 50%;
  left: 12rpx;
  display: flex;
  width: 80rpx;
  height: 68rpx;
  align-items: center;
  justify-content: flex-start;
  padding-left: 30rpx;
  background: transparent;
  opacity: 1;
  transform: translateY(-50%);
  transition: opacity 80ms ease;
}

.nav__back--pressed {
  background: transparent;
  opacity: 0.5;
}

.nav__back-icon {
  width: 21rpx;
  height: 21rpx;
  flex: 0 0 21rpx;
  border-bottom: 4rpx solid #68784b;
  border-left: 4rpx solid #68784b;
  border-radius: 2rpx;
  transform: rotate(45deg);
  transform-origin: center;
}

.nav--light:not(.nav--solid) .nav__back-icon {
  border-color: #ffffff;
}

.nav__location {
  position: absolute;
  z-index: 3;
  top: 50%;
  left: 24rpx;
  display: flex;
  max-width: 176rpx;
  height: 54rpx;
  min-width: 0;
  align-items: center;
  margin: 0;
  padding: 0 17rpx 0 14rpx;
  overflow: hidden;
  border: 1rpx solid rgba(255, 255, 255, 0.48);
  border-radius: 27rpx;
  background: rgba(34, 32, 28, 0.25);
  color: #ffffff;
  line-height: 1;
  transform: translateY(-50%);
}

.nav__location::after {
  border: 0;
}

.nav--solid .nav__location {
  border-color: rgba(91, 105, 64, 0.14);
  background: rgba(255, 255, 255, 0.94);
  color: #5f713f;
  box-shadow: 0 5rpx 18rpx rgba(50, 46, 38, 0.05);
}

.nav__location--pressed {
  opacity: 0.78;
}

.nav__location-icon {
  width: 28rpx;
  height: 28rpx;
  flex: 0 0 28rpx;
  margin-right: 8rpx;
}

.nav__location-text {
  min-width: 0;
  overflow: hidden;
  font-size: 20rpx;
  font-weight: 600;
  letter-spacing: 1rpx;
  text-overflow: ellipsis;
  white-space: nowrap;
}

@media (max-width: 360px) {
  .nav__back {
    left: 8rpx;
    width: 74rpx;
    height: 64rpx;
    padding-left: 28rpx;
  }

  .nav__back-icon {
    width: 20rpx;
    height: 20rpx;
    flex-basis: 20rpx;
  }

  .nav__location {
    left: 16rpx;
    max-width: 144rpx;
    height: 50rpx;
    padding-right: 12rpx;
    padding-left: 10rpx;
  }

  .nav__location-icon {
    width: 25rpx;
    height: 25rpx;
    flex-basis: 25rpx;
  }

  .nav__brand-image {
    width: 116rpx;
    height: 43rpx;
    transform: translateY(-8rpx);
  }
}
