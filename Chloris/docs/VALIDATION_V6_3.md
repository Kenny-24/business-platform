# V6.3 验证记录

已完成以下自动检查：

- 小程序与云函数全部 JavaScript 文件通过 `node -c` 语法检查；
- 项目全部 JSON 文件通过解析检查；
- 页面、组件、图片和相对 `require` 路径完成存在性检查；
- Web 管理中心通过 `npm run build` 正式构建；
- 交付包未包含 `node_modules`、系统缓存或 Git 仓库数据。

仍需在微信开发者工具和真机中完成最终授权、云环境、客服能力与视觉尺寸联调。
