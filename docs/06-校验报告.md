# 6. 校验报告

- JSON 文件：20 个，全部解析通过；
- JavaScript 源文件：24 个，Node 语法检查通过；
- Web 后台：`npm install` 和 `npm run build` 已成功完成；
- Web 构建产物：已生成 `huayu-admin-web/dist/`；
- 小程序页面：5 个主页面文件完整；
- 小程序组件：4 个公共组件文件完整；
- 页面组件引用：路径检查通过；
- 云函数：`getHomeData` 与 `adminApi` 均含 package-lock.json；
- 管理集合：代码会给出缺失集合的明确提示，不使用不确定的自动建集合 API。

仍需在你的账号环境中完成：

1. 在数据库创建 `products`、`admins`、`banners`、`atlas` 四个空集合；
2. 上传部署两个云函数；
3. 配置 Web Publishable Key；
4. 开启用户名密码登录；
5. 在微信开发者工具中真机编译和预览。
