# 数据库与部署

至少创建空集合：

```text
admins
products
banners
atlas
calendarEvents
```

建议设置为仅管理端可读写。小程序公开内容由 `getHomeData` 云函数统一读取。

部署顺序：

1. 部署 `adminApi`；
2. 部署 `getHomeData`；
3. 启动 Web 后台；
4. 在后台配置商品、横幅、节日和图鉴；
5. 小程序下拉刷新或清缓存重编译。
